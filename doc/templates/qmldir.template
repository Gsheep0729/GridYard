# =====================================================================
#  qmldir 文件样板（手写版）
#  对应规范：GY-CS-002 §二.3（脚注）
#
#  使用场景：
#    99% 的情况你应该用 qt_add_qml_module()，让 CMake 自动生成
#    qmldir，不要手写。
#
#    仅在以下情形需要手写：
#      - 维护纯 QML 模块（无 C++ 类型）且不走 CMake
#      - 老项目迁移过来还来不及改造 CMake
#      - QML UI 原型项目（.qmlproject + qml 工具直接运行）
#
#  qmldir 每条命令独占一行；以 '#' 开头的是注释。
# =====================================================================

# ----------------------------------------------------------------------
# 1) 模块标识符（必须，全模块文件的第一条命令）
# 格式：module <URI>
# URI 规则：dot-notation lowercase，对应文件系统目录层级
# ----------------------------------------------------------------------
module cqnu.gridyard.<<MODULE_NAME>>

# ----------------------------------------------------------------------
# 2) C++ 插件库（仅当模块包含 C++ 类型时需要）
# 格式：plugin <插件库名（不带 lib 前缀和 .so 后缀）>
# ----------------------------------------------------------------------
# plugin <<TARGET_NAME>>plugin

# ----------------------------------------------------------------------
# 3) 插件入口 C++ 类（与 plugin 配套使用）
# 格式：classname <C++ 类名>
# ----------------------------------------------------------------------
# classname <<PluginClassName>>

# ----------------------------------------------------------------------
# 4) 类型描述文件（供 Qt Creator 等工具做补全 / 检查）
# 通常由 qt_add_qml_module 自动生成
# ----------------------------------------------------------------------
# typeinfo plugins.qmltypes

# ----------------------------------------------------------------------
# 5) QML 对象类型声明
# 格式：<TypeName> <Version> <FilePath>
# - 类型名首字母必须大写
# - 一个类型可以有多个版本声明
# ----------------------------------------------------------------------
DeviceCard       1.0 DeviceCard.qml
TransferTaskCard 1.0 TransferTaskCard.qml

# ----------------------------------------------------------------------
# 6) 内部类型（仅模块内部使用，对外不可见）
# 格式：internal <TypeName> <FilePath>
# ----------------------------------------------------------------------
internal HelperBubble HelperBubble.qml

# ----------------------------------------------------------------------
# 7) Singleton 类型
# 格式：singleton <TypeName> <Version> <FilePath>
# 对应的 .qml 文件首行必须有 "pragma Singleton"
# ----------------------------------------------------------------------
# singleton ThemeManager 1.0 ThemeManager.qml

# ----------------------------------------------------------------------
# 8) JavaScript 资源
# 格式：<Identifier> <Version> <FilePath>
# ----------------------------------------------------------------------
# Format 1.0 utils/format.js

# ----------------------------------------------------------------------
# 9) 模块依赖（仅当依赖关系被隐藏时需要）
# 格式：depends <URI> <Version>
# ----------------------------------------------------------------------
# depends QtQuick 6.5

# ----------------------------------------------------------------------
# 10) 设计器支持（仅当该插件经过 Qt Designer 集成测试时声明）
# ----------------------------------------------------------------------
# designersupported
